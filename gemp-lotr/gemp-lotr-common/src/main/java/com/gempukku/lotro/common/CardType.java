package com.gempukku.lotro.common;

public enum CardType implements Filterable {
    ADVENTURE, ALLY, COMPANION, CONDITION, FOLLOWER, MINION, POSSESSION, SITE, THE_ONE_RING,
    ARTIFACT, EVENT;
}
